# =============================================================================
# Variables de entorno para biciservice.cc
# =============================================================================
# Este archivo sirve como template. Copialo a .env y ajusta los valores:
#   cp .env.example .env
#
# El archivo .env esta en .gitignore, asi que cada entorno (tu maquina local
# y el VPS de produccion) tiene su propio .env con valores distintos.
# Un "git pull" NUNCA sobreescribe tu .env.
# =============================================================================

# --- Flask ---
SECRET_KEY=change-me
DATABASE_URL=postgresql+psycopg://postgres:postgres@db:5432/biciservice_cc
UPLOAD_FOLDER=/app/app/static/uploads
ASSET_VERSION=dev
SERVICE_WORKER_ENABLED=true

# Seguridad de cookies (false en dev, true en prod con HTTPS)
SESSION_COOKIE_SECURE=false
REMEMBER_COOKIE_SECURE=false

# Debug mode (1 en dev, 0 o vacio en prod)
FLASK_DEBUG=1
# Nota: con FLASK_DEBUG=1 el service worker se desactiva automaticamente.

# URL publica de la app (usar https en produccion)
APP_BASE_URL=http://localhost:5001

# --- Email / SMTP ---
# Ejemplo Gmail:
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=tu_cuenta@gmail.com
# SMTP_PASSWORD=tu_app_password
# SMTP_USE_TLS=true
MAIL_FROM=no-reply@example.com
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_USE_TLS=true
SMTP_USE_SSL=false
MAIL_TIMEOUT_SECONDS=10

# Si quieres desactivar envios reales (tests/local), usar true
MAIL_SUPPRESS_SEND=false

# Opcional: email para avisos de nuevos registros pendientes
ADMIN_NOTIFICATION_EMAIL=

# --- PostgreSQL (usado por el contenedor db) ---
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=biciservice_cc

# Ejemplo de variables en producción
# # Flask
# FLASK_DEBUG=0
# SECRET_KEY=<clave-larga-segura>

# # Base de datos
# DATABASE_URL=postgresql+psycopg://<user>:<pass>@<host>:5432/<db>

# # Assets / PWA
# ASSET_VERSION=<release-id-unico>   # ej: git SHA, tag o build number
# SERVICE_WORKER_ENABLED=true

# # Cookies seguras (siempre en HTTPS)
# SESSION_COOKIE_SECURE=true
# REMEMBER_COOKIE_SECURE=true

# # URL pública
# APP_BASE_URL=https://tu-dominio.com
